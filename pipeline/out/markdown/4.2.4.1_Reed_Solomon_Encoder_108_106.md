---
{
  "section": "4.2.4.1",
  "section_title": "Reed Solomon Encoder (108,106)",
  "module": "Physical Layer Gen2020",
  "depth": 3,
  "page_start": 104,
  "page_end": 105,
  "content_type": "figure",
  "register_address": null,
  "tables": [
    {
      "id": "4-10",
      "title": "Generator polynomial coefficients for RS(108,106)"
    }
  ],
  "figures": [
    {
      "id": "4-9",
      "title": "Block diagram of RS(108,106) parity generation"
    }
  ],
  "source": "ASA_Technical_Specification_ver2.0.pdf"
}
---

# 4.2.4.1 Reed Solomon Encoder (108,106)

4.2.4.1 Reed Solomon Encoder (108,106)
A group of 106 bytes is encoded using a Reed-Solomon encoder operating over the Galois Field
GF(2) where the symbol size is 8 bits (or one byte). The encoder processes k = 106 RS-FEC message
symbols to generate 2t = 2 RS-FEC parity symbols, which are then appended to the message to
produce a codeword of n = k + 2t = 108 RS-FEC symbols.
A Reed Solomon symbol b = bα+ bα+ bα+ bα+ bα+ bα+ bα+ bwhere bis either 0 or 1.
i
The Reed Solomon symbol b as defined above can be represented by a hexadecimal value by
concatenating {b, b, b, b, b, b, b, b} where bis the most significant bit. Note thatαis
represented as 0x02.
For RS(108, 106) the message is m(x) = m x + m x + m x + … + mx + mand the
codeword c(x) = m(x) x+ p(x), where p(x) = p(x) = px + pis the parity generated by the generating
polynomial,
𝑔(𝑥) = ∏(𝑥 − 𝛼𝑖)= 𝑔𝑥+ 𝑔𝑥 + 𝑔
𝑖=0
Equation 4-5: Generator polynomial for RS(108,106)
That is, p(x) is the remainder when m(x) is divided by g(x).
And whereαis a primitive element in the finite field defined by the primitive polynomial
x+ x+ x+ x+ 1.
The generator coefficients are
i
g | 0x02 | 0x03 | 0x01
i
Table 4-10: Generator polynomial coefficients for RS(108,106)
The shift register in the diagram below illustrates the principle structure for the codeword generation
where the addition and multiplication shown are Galois addition and multiplication and all pare
i
initialized to 0x00.
g | g | g
p | p | m , m , m ,, m, m
mux
c , c c ,, c, c= m , m , m ,, m, m, p, p
106,
Figure 4-9: Block diagram of RS(108,106) parity generation
